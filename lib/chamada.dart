import 'package:flutter/material.dart';

class Chamada extends StatelessWidget {
  const Chamada({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text("Página de Chamadas"),
    );
  }
}